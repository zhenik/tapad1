app {
  kafka-bootstrap-servers = "test:1234"
  kafka-bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
  input-topic = "in"
  input-topic = ${?KAFKA_INPUT_TOPIC}
  output-topic = "out"
  output-topic = ${?KAFKA_OUTPUT_TOPIC}
}